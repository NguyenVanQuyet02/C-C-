#include<bits/stdc++.h>
using namespace std;
int main(){
    float x;
    cout<<"nhap x ";
    cin>>x;
    cout<<"\n f(x) = "<< (pow(x,2) + exp(fabs(x)) + pow(sin(x),2)) / pow((x*x + 1),(float)1/5);
    return 0;
}
